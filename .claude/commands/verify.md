Run the full verification pipeline and report results.

1. Run `pnpm check` — report any type errors
2. Run `pnpm test` — report any test failures
3. Run `pnpm build` — report any build errors
4. Summarize: pass/fail for each step, and list any issues that need fixing
